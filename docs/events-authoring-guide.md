# Events Authoring Guide

The single authoritative guide for creating a new event now lives in:

- [`docs/add-new-event.md`](./add-new-event.md)

Use that file when you want:
- the exact folder structure
- the JSON contract for `package.json`, `research.json`, and `qa.json`
- the allowed keys and their purpose
- the build, validation, and publish workflow
- an AI-ready checklist for adding new events safely

This file stays intentionally short so the authoring workflow has one source of truth instead of multiple overlapping guides.

