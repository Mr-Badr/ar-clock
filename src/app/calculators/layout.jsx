import './calculators.css';
import CalculatorAdLayout from '@/components/ads/CalculatorAdLayout';

export default function CalculatorsLayout({ children }) {
  return <CalculatorAdLayout>{children}</CalculatorAdLayout>;
}
