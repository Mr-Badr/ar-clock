'use client';
/**
 * SearchCityWrapper.client.jsx
 *
 * Transparent pass-through to SearchCity.
 * This wrapper exists as a stable import alias so pages don't need changing
 * if the loading strategy changes in the future.
 */
export { default } from './SearchCity.client';
